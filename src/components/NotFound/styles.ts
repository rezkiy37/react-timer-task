import { makeStyles } from '@material-ui/core/styles'

export const useStylesNotFound = makeStyles({
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
})
